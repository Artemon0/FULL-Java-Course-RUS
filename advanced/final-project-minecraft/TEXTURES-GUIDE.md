# 🎨 Руководство по добавлению текстур

## 📁 Куда класть текстуры

```
advanced/final-project-minecraft/
└── src/
    └── main/
        └── resources/
            └── textures/
                ├── atlas.png          ← Сюда кладите атлас текстур
                ├── blocks/            ← Или отдельные текстуры сюда
                │   ├── grass.png
                │   ├── dirt.png
                │   └── stone.png
                └── README.md          ← Инструкция
```

## 🎯 Быстрый старт

### Вариант 1: Использовать готовый атлас (рекомендуется)

1. **Скачайте или создайте атлас текстур** (64x32 пикселя)
   - 4 текстуры в ширину × 2 в высоту
   - Каждая текстура 16×16 пикселей

2. **Положите файл** `atlas.png` в:
   ```
   src/main/resources/textures/atlas.png
   ```

3. **Готово!** Текстуры будут загружены автоматически

### Вариант 2: Создать простые цветные текстуры

Если у вас нет текстур, создайте простые одноцветные квадраты 16×16:

**Grass (Трава):**
- Цвет: #7CBD6B (зелёный)

**Dirt (Земля):**
- Цвет: #8B6F47 (коричневый)

**Stone (Камень):**
- Цвет: #7F7F7F (серый)

**Wood (Дерево):**
- Цвет: #8B5A2B (коричневый)

## 🛠️ Инструменты для создания текстур

### Онлайн (бесплатно)
1. **Piskel** - https://www.piskelapp.com/
   - Pixel art редактор
   - Работает в браузере
   - Экспорт в PNG

2. **Photopea** - https://www.photopea.com/
   - Аналог Photoshop
   - Бесплатный
   - Работает в браузере

### Программы
1. **GIMP** - бесплатный редактор изображений
2. **Aseprite** - платный pixel art редактор ($20)
3. **Paint.NET** - бесплатный (Windows)

## 📦 Где скачать готовые текстуры

### Бесплатные ресурсы
1. **OpenGameArt.org**
   - Поиск: "minecraft textures" или "voxel textures"
   - Лицензия: CC0 или CC-BY

2. **itch.io**
   - Поиск: "texture pack"
   - Много бесплатных наборов

3. **Kenney.nl**
   - Раздел: Voxel Pack
   - Бесплатно для любых целей

### ⚠️ Важно
- НЕ используйте оригинальные текстуры Minecraft для публикации
- Проверяйте лицензию скачанных текстур
- Для обучения можно использовать любые

## 🎨 Создание атласа текстур

### Структура атласа 64×32 (4×2 текстуры)

```
┌─────────┬─────────┬─────────┬─────────┐
│ Grass   │ Dirt    │ Stone   │ Wood    │  ← Ряд 0
│ (0,0)   │ (1,0)   │ (2,0)   │ (3,0)   │
├─────────┼─────────┼─────────┼─────────┤
│ Leaves  │ Sand    │ Water   │ Bedrock │  ← Ряд 1
│ (0,1)   │ (1,1)   │ (2,1)   │ (3,1)   │
└─────────┴─────────┴─────────┴─────────┘
```

### Как создать в Photoshop/GIMP:

1. Создайте новый файл 64×32 пикселя
2. Разделите на сетку 16×16
3. Нарисуйте или вставьте текстуры в каждую ячейку
4. Сохраните как PNG

### Как создать в Piskel:

1. Создайте спрайт 64×32
2. Нарисуйте текстуры
3. Export → PNG

## 💻 Пример кода для загрузки

Создайте класс `Texture.java`:

```java
package com.minecraft.render;

import org.lwjgl.BufferUtils;
import org.lwjgl.stb.STBImage;
import java.nio.ByteBuffer;
import java.nio.IntBuffer;
import static org.lwjgl.opengl.GL11.*;

public class Texture {
    private int id;
    private int width;
    private int height;

    public Texture(String path) {
        // Загрузка текстуры через STB
        IntBuffer w = BufferUtils.createIntBuffer(1);
        IntBuffer h = BufferUtils.createIntBuffer(1);
        IntBuffer channels = BufferUtils.createIntBuffer(1);

        // Загрузка из resources
        String fullPath = "src/main/resources/" + path;
        ByteBuffer image = STBImage.stbi_load(fullPath, w, h, channels, 4);

        if (image == null) {
            throw new RuntimeException("Не удалось загрузить текстуру: " + path);
        }

        width = w.get(0);
        height = h.get(0);

        // Создание OpenGL текстуры
        id = glGenTextures();
        glBindTexture(GL_TEXTURE_2D, id);

        glTexImage2D(GL_TEXTURE_2D, 0, GL_RGBA, width, height, 
                     0, GL_RGBA, GL_UNSIGNED_BYTE, image);

        // Настройки фильтрации (NEAREST для pixel art)
        glTexParameteri(GL_TEXTURE_2D, GL_TEXTURE_MIN_FILTER, GL_NEAREST);
        glTexParameteri(GL_TEXTURE_2D, GL_TEXTURE_MAG_FILTER, GL_NEAREST);

        // Освобождение памяти
        STBImage.stbi_image_free(image);

        System.out.println("Текстура загружена: " + path + " (" + width + "x" + height + ")");
    }

    public void bind() {
        glBindTexture(GL_TEXTURE_2D, id);
    }

    public void unbind() {
        glBindTexture(GL_TEXTURE_2D, 0);
    }

    public int getId() {
        return id;
    }
}
```

## 🎯 Использование в Renderer

```java
// В конструкторе Renderer
private Texture blockAtlas;

public Renderer(Window window, Camera camera) {
    this.window = window;
    this.camera = camera;
    
    // Загрузка текстур
    blockAtlas = new Texture("textures/atlas.png");
    
    initShaders();
    initCube();
}

// В методе render
public void render(World world) {
    glUseProgram(shaderProgram);
    
    // Привязать текстуру
    blockAtlas.bind();
    
    // ... рендеринг кубов ...
    
    blockAtlas.unbind();
}
```

## 📝 Добавление UV координат

Обновите вершины куба, добавив UV координаты:

```java
// Формат: позиция (3) + цвет (3) + UV (2) = 8 float на вершину
float[] vertices = {
    // Позиция          Цвет              UV
    -0.5f, -0.5f, -0.5f, 1.0f, 1.0f, 1.0f, 0.0f, 0.0f,
     0.5f, -0.5f, -0.5f, 1.0f, 1.0f, 1.0f, 0.25f, 0.0f,
     0.5f,  0.5f, -0.5f, 1.0f, 1.0f, 1.0f, 0.25f, 0.5f,
    // ... и т.д.
};

// Обновите атрибуты
glVertexAttribPointer(0, 3, GL_FLOAT, false, 8 * Float.BYTES, 0);
glVertexAttribPointer(1, 3, GL_FLOAT, false, 8 * Float.BYTES, 3 * Float.BYTES);
glVertexAttribPointer(2, 2, GL_FLOAT, false, 8 * Float.BYTES, 6 * Float.BYTES);
glEnableVertexAttribArray(2); // UV координаты
```

## 🚀 Быстрый тест

1. Создайте простой файл `test.png` (16×16, любой цвет)
2. Положите в `src/main/resources/textures/test.png`
3. Загрузите: `new Texture("textures/test.png")`
4. Если работает - добавляйте остальные текстуры!

## 🐛 Решение проблем

### Текстура не загружается
- Проверьте путь к файлу
- Убедитесь что файл в формате PNG
- Проверьте консоль на ошибки

### Текстура размытая
- Используйте `GL_NEAREST` вместо `GL_LINEAR`
- Для pixel art всегда NEAREST

### Текстура перевёрнута
- STB загружает текстуры перевёрнутыми
- Добавьте: `STBImage.stbi_set_flip_vertically_on_load(true);`

---

**Готово! Теперь вы знаете как добавить текстуры в Minecraft Clone! 🎨**

Начните с простых цветных квадратов, потом замените на красивые текстуры!

