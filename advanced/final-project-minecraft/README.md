# 🎮 ФИНАЛЬНЫЙ ПРОЕКТ: 3D Игра в стиле Minecraft

```
╔════════════════════════════════════════════════════════════╗
║                                                            ║
║           MINECRAFT CLONE - 3D VOXEL GAME                  ║
║           Финальный проект курса Java                      ║
║                                                            ║
╚════════════════════════════════════════════════════════════╝
```

## 📋 Описание

Полноценная 3D игра в стиле Minecraft с использованием **LWJGL 3** (Lightweight Java Game Library).

### Основные возможности:
- ✅ **3D рендеринг** с OpenGL
- ✅ **Генерация мира** с помощью Perlin Noise
- ✅ **Система блоков** (воксели)
- ✅ **Управление игроком** (WASD, мышь, прыжок)
- ✅ **Физика** (гравитация, коллизии)
- ✅ **Камера от первого лица**
- ✅ **Размещение/разрушение блоков**
- ✅ **Инвентарь**
- ✅ **Система сохранений**
- ✅ **Текстуры и шейдеры**

## 🏗️ Архитектура проекта

```
minecraft-clone/
├── pom.xml                          # Maven конфигурация
├── src/main/java/
│   ├── Main.java                    # Точка входа
│   ├── core/
│   │   ├── Window.java              # Окно игры
│   │   ├── GameLoop.java            # Игровой цикл
│   │   └── Input.java               # Обработка ввода
│   ├── world/
│   │   ├── World.java               # Игровой мир
│   │   ├── Chunk.java               # Чанк (16x256x16)
│   │   ├── Block.java               # Блок
│   │   ├── BlockType.java           # Типы блоков
│   │   └── WorldGenerator.java      # Генератор мира
│   ├── entity/
│   │   ├── Entity.java              # Базовая сущность
│   │   ├── Player.java              # Игрок
│   │   └── Camera.java              # Камера
│   ├── physics/
│   │   ├── PhysicsEngine.java       # Физический движок
│   │   ├── AABB.java                # Bounding Box
│   │   └── Collision.java           # Коллизии
│   ├── render/
│   │   ├── Renderer.java            # Рендерер
│   │   ├── Mesh.java                # 3D меш
│   │   ├── Shader.java              # Шейдеры
│   │   ├── Texture.java             # Текстуры
│   │   └── ChunkMesh.java           # Меш чанка
│   ├── inventory/
│   │   ├── Inventory.java           # Инвентарь
│   │   └── ItemStack.java           # Стак предметов
│   ├── save/
│   │   ├── SaveManager.java         # Сохранения
│   │   └── WorldData.java           # Данные мира
│   └── util/
│       ├── Vector3f.java            # 3D вектор
│       ├── Matrix4f.java            # 4x4 матрица
│       └── PerlinNoise.java         # Шум Перлина
└── src/main/resources/
    ├── shaders/
    │   ├── vertex.glsl              # Вершинный шейдер
    │   └── fragment.glsl            # Фрагментный шейдер
    └── textures/
        └── blocks.png               # Атлас текстур
```

## 🔧 Технологии

### Обязательные зависимости:
- **Java 17+**
- **LWJGL 3.3.3** - OpenGL, GLFW, STB
- **JOML 1.10.5** - Java OpenGL Math Library
- **Maven** - сборка проекта

### Maven зависимости (pom.xml):
```xml
<dependencies>
    <!-- LWJGL Core -->
    <dependency>
        <groupId>org.lwjgl</groupId>
        <artifactId>lwjgl</artifactId>
        <version>3.3.3</version>
    </dependency>
    
    <!-- LWJGL OpenGL -->
    <dependency>
        <groupId>org.lwjgl</groupId>
        <artifactId>lwjgl-opengl</artifactId>
        <version>3.3.3</version>
    </dependency>
    
    <!-- LWJGL GLFW -->
    <dependency>
        <groupId>org.lwjgl</groupId>
        <artifactId>lwjgl-glfw</artifactId>
        <version>3.3.3</version>
    </dependency>
    
    <!-- JOML -->
    <dependency>
        <groupId>org.joml</groupId>
        <artifactId>joml</artifactId>
        <version>1.10.5</version>
    </dependency>
</dependencies>
```

## 🚀 Компиляция и запуск

### Быстрый запуск (IntelliJ IDEA):
1. Откройте проект в IntelliJ IDEA
2. Дождитесь загрузки Maven зависимостей
3. Запустите `Main.java`

Подробнее: [FIRST-RUN.md](FIRST-RUN.md)

### С Maven:
```bash
mvn clean package
mvn exec:java
```

### Вручную:
```bash
javac -cp "lib/*" src/main/java/**/*.java
java -cp "lib/*:src/main/java" Main
```

## 🎨 Добавление текстур

Текстуры кладутся в `src/main/resources/textures/`

Подробное руководство: [TEXTURES-GUIDE.md](TEXTURES-GUIDE.md)

### Быстрый старт:
1. Создайте или скачайте текстуры (16×16 PNG)
2. Положите в `src/main/resources/textures/`
3. Загрузите в коде через класс `Texture`

### Где взять текстуры:
- **OpenGameArt.org** - бесплатные ресурсы
- **itch.io** - много asset pack'ов
- **Kenney.nl** - качественные бесплатные ассеты
- Создайте свои в Piskel или GIMP

## 🎮 Управление

### Движение:
- `W` - вперёд
- `S` - назад
- `A` - влево
- `D` - вправо
- `Space` - прыжок
- `Shift` - ускорение

### Камера:
- `Мышь` - поворот камеры
- `Scroll` - выбор блока в инвентаре

### Действия:
- `ЛКМ` - разрушить блок
- `ПКМ` - поставить блок
- `E` - инвентарь
- `ESC` - меню/выход

## 📊 Основные компоненты

### 1. Система блоков

Каждый блок имеет тип и позицию в мире:

```java
public enum BlockType {
    AIR(0, false),
    GRASS(1, true),
    DIRT(2, true),
    STONE(3, true),
    WOOD(4, true),
    LEAVES(5, true),
    SAND(6, true),
    WATER(7, false);
    
    private final int id;
    private final boolean solid;
}
```

### 2. Чанки

Мир разделён на чанки 16x256x16 блоков для оптимизации:

```java
public class Chunk {
    public static final int SIZE = 16;
    public static final int HEIGHT = 256;
    
    private Block[][][] blocks;
    private ChunkMesh mesh;
    private boolean needsRebuild;
}
```

### 3. Генерация мира

Используется Perlin Noise для создания реалистичного ландшафта:

```java
public class WorldGenerator {
    private PerlinNoise noise;
    
    public void generateChunk(Chunk chunk) {
        for (int x = 0; x < 16; x++) {
            for (int z = 0; z < 16; z++) {
                int height = getHeight(x, z);
                generateColumn(chunk, x, z, height);
            }
        }
    }
}
```

### 4. Физика

Простая физика с гравитацией и коллизиями:

```java
public class PhysicsEngine {
    private static final float GRAVITY = -20.0f;
    
    public void update(Player player, float delta) {
        applyGravity(player, delta);
        checkCollisions(player);
    }
}
```

### 5. Рендеринг

OpenGL рендеринг с шейдерами:

```java
public class Renderer {
    private Shader shader;
    
    public void render(World world, Camera camera) {
        shader.bind();
        shader.setUniform("viewMatrix", camera.getViewMatrix());
        shader.setUniform("projectionMatrix", camera.getProjectionMatrix());
        
        for (Chunk chunk : world.getVisibleChunks()) {
            chunk.getMesh().render();
        }
    }
}
```

## 🎨 Оптимизация

### Greedy Meshing
Объединение соседних граней для уменьшения количества полигонов.

### Frustum Culling
Рендеринг только видимых чанков.

### Occlusion Culling
Не рендерить скрытые грани блоков.

### Chunk Loading
Асинхронная загрузка чанков в отдельном потоке.

## 📝 Этапы разработки

### Этап 1: Основа (Неделя 1)
- ✅ Настройка LWJGL
- ✅ Создание окна
- ✅ Игровой цикл
- ✅ Базовый рендеринг

### Этап 2: Блоки (Неделя 2)
- ✅ Система блоков
- ✅ Чанки
- ✅ Рендеринг кубов
- ✅ Текстуры

### Этап 3: Мир (Неделя 3)
- ✅ Генерация ландшафта
- ✅ Perlin Noise
- ✅ Биомы
- ✅ Деревья

### Этап 4: Игрок (Неделя 4)
- ✅ Камера FPS
- ✅ Управление
- ✅ Физика
- ✅ Коллизии

### Этап 5: Взаимодействие (Неделя 5)
- ✅ Размещение блоков
- ✅ Разрушение блоков
- ✅ Raycast
- ✅ Инвентарь

### Этап 6: Полировка (Неделя 6)
- ✅ Сохранения
- ✅ Меню
- ✅ Оптимизация
- ✅ Баг-фиксы

## 🎓 Что вы изучите

1. **OpenGL** - 3D графика
2. **Шейдеры** - GLSL программирование
3. **3D математика** - векторы, матрицы
4. **Оптимизация** - производительность
5. **Архитектура** - большой проект
6. **Многопоточность** - асинхронная загрузка

## 🚀 Идеи для расширения

1. Добавить мобов (враги, животные)
2. Реализовать крафтинг
3. Добавить день/ночь цикл
4. Создать систему освещения
5. Добавить звуки
6. Реализовать мультиплеер
7. Добавить пещеры
8. Создать систему биомов

---

**Поздравляем с завершением курса Java! 🎉**

Вы создали полноценную 3D игру с нуля!
