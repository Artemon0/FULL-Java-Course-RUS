# 🔧 Быстрое исправление проблем

## Проблема: Синий экран + не захватывает курсор

### Причина:
1. Renderer был пустой - ничего не рисовал
2. Не было захвата курсора в Window
3. Camera не предоставляла матрицы
4. Player не обрабатывал мышь

### Решение (УЖЕ ПРИМЕНЕНО):

#### 1. Window.java
```java
// Добавлено в init():
glfwSetInputMode(windowHandle, GLFW_CURSOR, GLFW_CURSOR_DISABLED);
if (glfwRawMouseMotionSupported()) {
    glfwSetInputMode(windowHandle, GLFW_RAW_MOUSE_MOTION, GLFW_TRUE);
}
```

#### 2. Player.java
```java
// Добавлены поля:
private float yaw = 0;
private float pitch = 0;

// Добавлен метод:
private void updateMouseInput(Window window) {
    // Обработка движения мыши
}

// Добавлены геттеры:
public float getYaw()
public float getPitch()
public Vector3f getPosition()
```

#### 3. Camera.java
```java
// Реализованы матрицы:
private Matrix4f projectionMatrix;
private Matrix4f viewMatrix;

public Matrix4f getProjectionMatrix()
public Matrix4f getViewMatrix()
```

#### 4. Renderer.java
```java
// Добавлены:
- Шейдеры (vertex + fragment)
- Тестовый куб (цветной)
- Рендеринг с матрицами
```

## Как проверить что всё работает:

1. Запустите игру через IntelliJ IDEA
2. Вы должны увидеть:
   - ✅ Голубое небо
   - ✅ Цветной куб перед вами
   - ✅ Курсор исчезнет (захвачен)
3. Попробуйте:
   - Подвигать мышью - камера должна вращаться
   - WASD - движение
   - Space - прыжок

## Если не работает:

### Проблема: Ошибки компиляции
**Решение:** Обновите Maven зависимости в IntelliJ:
- Правой кнопкой на pom.xml → Maven → Reload Project

### Проблема: Чёрный экран вместо синего
**Решение:** Проверьте консоль на ошибки OpenGL. Возможно нужно обновить драйверы видеокарты.

### Проблема: Курсор не захватывается
**Решение:** Убедитесь что Window.java содержит строки с glfwSetInputMode

### Проблема: Куб не виден
**Решение:** Проверьте что в консоли есть "Шейдеры скомпилированы успешно"

## Технические детали:

- **OpenGL версия:** 3.3 Core Profile
- **Шейдеры:** GLSL 330
- **Тестовый куб:** 36 вершин (6 граней × 2 треугольника)
- **Цвета:** RGB для каждой грани
- **Позиция куба:** (0, 0, -3) - 3 единицы перед камерой
