# 🎨 У меня только одна текстура (dirt.png)

## ✅ Это нормально!

Можно начать с одной текстуры. Все блоки будут использовать её.

## 📁 Куда положить

Положите ваш `dirt.png` в:
```
advanced/final-project-minecraft/src/main/resources/textures/dirt.png
```

## 🚀 Что произойдёт

При запуске игры вы увидите:
```
Загрузка текстур...
⚠️ Не удалось загрузить текстуру: grass
   Путь: textures/grass.png
   Ошибка: Текстура не найдена: ...
✅ Текстура загружена: textures/dirt.png
   Размер: 16×16 пикселей
   OpenGL ID: 1
⚠️ Не удалось загрузить текстуру: stone
   ...
✅ Загружено текстур: 1
   Доступные: dirt
```

**Это нормально!** Предупреждения можно игнорировать.

## 💡 Как использовать

Все блоки будут использовать текстуру `dirt`:

```java
// В Renderer.java
private TextureManager textureManager;

public Renderer(Window window, Camera camera) {
    textureManager = new TextureManager();
    // Загрузится только dirt.png
}

public void render(World world) {
    // Используем dirt для всех блоков
    textureManager.bindTexture("dirt");
    
    // Рендерим все кубы
    // ...
}
```

## 🎨 Добавление других текстур

Когда будут готовы другие текстуры:

1. Положите их в `src/main/resources/textures/`
2. Назовите: `grass.png`, `stone.png`, `wood.png`
3. Перезапустите игру
4. Они загрузятся автоматически!

## 🔧 Если хотите использовать dirt для всех блоков

Можно скопировать `dirt.png` несколько раз:

```bash
# В папке textures/
copy dirt.png grass.png
copy dirt.png stone.png
copy dirt.png wood.png
```

Или в PowerShell:
```powershell
cd src/main/resources/textures
Copy-Item dirt.png grass.png
Copy-Item dirt.png stone.png
Copy-Item dirt.png wood.png
```

Тогда все блоки будут с текстурой земли (пока не замените на нормальные).

## 📊 Что будет в игре

- ✅ Текстура `dirt` загрузится
- ✅ Можно использовать для рендеринга
- ⚠️ Другие текстуры не загрузятся (это ок)
- ✅ Игра не упадёт

## 🎯 Рекомендация

**Для начала одной текстуры достаточно!**

Можете:
1. Использовать `dirt` для всех блоков
2. Или скопировать `dirt.png` в другие имена
3. Или постепенно добавлять новые текстуры

---

**Всё работает с одной текстурой! Добавляйте остальные когда будут готовы! 🎨**
