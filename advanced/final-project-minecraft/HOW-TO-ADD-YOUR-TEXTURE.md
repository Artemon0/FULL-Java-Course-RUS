# 🎨 Как добавить вашу PNG текстуру

## ⚡ Быстрая инструкция (2 минуты)

### Шаг 1: Скопируйте вашу PNG картинку

Положите вашу PNG картинку в эту папку:
```
advanced/final-project-minecraft/src/main/resources/textures/
```

### Шаг 2: Имя файла

**Можете оставить любое имя!** Например:
```
blocks.png
textures.png
minecraft.png
my_texture.png
```

Или переименуйте по содержимому:
```
atlas.png    (если это атлас - много текстур)
grass.png    (если это одна текстура травы)
dirt.png     (земля)
stone.png    (камень)
```

**Главное - запомните имя файла!**

### Шаг 3: Проверьте что файлы на месте

Положите ваши PNG файлы:
```
advanced/final-project-minecraft/src/main/resources/textures/
├── grass.png
├── dirt.png
├── stone.png
└── wood.png
```

**Можно положить хоть один файл - остальные необязательны!**

### Если у вас только один файл (например, dirt.png):
✅ Это нормально! Смотрите: [ONE-TEXTURE-ONLY.md](ONE-TEXTURE-ONLY.md)

### Шаг 4: Запустите игру

Классы `Texture.java` и `TextureManager.java` уже созданы!
Текстуры загрузятся автоматически!

## 🔍 Проверка размера текстуры

Откройте вашу PNG картинку и посмотрите размер:

### Если размер 16×16:
✅ Это одна текстура блока
- Используйте как `grass.png`, `dirt.png` и т.д.

### Если размер 64×32:
✅ Это атлас 4×2 (4 текстуры в ширину, 2 в высоту)
- Переименуйте в `atlas.png`
- Каждая текстура 16×16

### Если размер 128×128:
✅ Это атлас 8×8 (64 текстуры)
- Переименуйте в `atlas.png`
- Каждая текстура 16×16

### Если размер 256×256:
✅ Это большой атлас 16×16 (256 текстур)
- Переименуйте в `atlas.png`
- Каждая текстура 16×16

## 🎯 Использование отдельных текстур (БЕЗ атласа)

Класс `Texture.java` уже создан! Используйте так:

```java
// В Renderer.java
private Texture grassTexture;
private Texture dirtTexture;
private Texture stoneTexture;
private Texture woodTexture;

public Renderer(Window window, Camera camera) {
    this.window = window;
    this.camera = camera;
    
    // Загрузка отдельных текстур
    grassTexture = new Texture("textures/grass.png");
    dirtTexture = new Texture("textures/dirt.png");
    stoneTexture = new Texture("textures/stone.png");
    woodTexture = new Texture("textures/wood.png");
    
    initShaders();
    initCube();
}

public void render(World world) {
    glUseProgram(shaderProgram);
    
    // Рендерим блоки травы
    grassTexture.bind();
    // ... рендеринг кубов травы ...
    
    // Рендерим блоки земли
    dirtTexture.bind();
    // ... рендеринг кубов земли ...
    
    // Рендерим блоки камня
    stoneTexture.bind();
    // ... рендеринг кубов камня ...
}
```

### Преимущества отдельных текстур:
- ✅ Проще добавлять новые блоки
- ✅ Не нужно считать UV координаты
- ✅ Можно использовать разные размеры
- ✅ Легче редактировать

### Недостатки:
- ⚠️ Больше переключений текстур (медленнее)
- ⚠️ Больше файлов

**Для начала отдельные текстуры - отличный выбор!**

## 🐛 Если текстура не загружается

### Ошибка: "Текстура не найдена"
**Решение:**
1. Проверьте что файл в правильной папке
2. Проверьте имя файла (должно быть точно `atlas.png` или `grass.png`)
3. Проверьте расширение (должно быть `.png`, не `.PNG` или `.jpg`)

### Ошибка: "Не удалось загрузить текстуру"
**Решение:**
1. Убедитесь что файл в формате PNG
2. Откройте файл в Paint или другом редакторе
3. Сохраните заново как PNG
4. Попробуйте снова

### Текстура загружается, но не видна
**Решение:**
1. Проверьте что вы вызываете `blockTexture.bind()` перед рендерингом
2. Убедитесь что добавили UV координаты в вершины
3. Проверьте шейдеры (должны быть в `src/main/resources/shaders/`)

## 📊 Что вы увидите в консоли

При успешной загрузке:
```
✅ Текстура загружена: textures/atlas.png
   Размер: 64×32 пикселей
   OpenGL ID: 1
```

## 🎨 Формат вашей текстуры

### Если это атлас 4×2 (64×32):
```
┌────────┬────────┬────────┬────────┐
│ Текст1 │ Текст2 │ Текст3 │ Текст4 │  ← Ряд 0
│ (0,0)  │ (1,0)  │ (2,0)  │ (3,0)  │
├────────┼────────┼────────┼────────┤
│ Текст5 │ Текст6 │ Текст7 │ Текст8 │  ← Ряд 1
│ (0,1)  │ (1,1)  │ (2,1)  │ (3,1)  │
└────────┴────────┴────────┴────────┘
```

### UV координаты для атласа:
```java
// Для текстуры в позиции (x, y)
float u = x / 4.0f;        // 0.0, 0.25, 0.5, 0.75
float v = y / 2.0f;        // 0.0, 0.5
float uSize = 1.0f / 4.0f; // 0.25
float vSize = 1.0f / 2.0f; // 0.5
```

## 🚀 Быстрый тест

1. Положите вашу PNG в `src/main/resources/textures/atlas.png`
2. Запустите игру
3. Проверьте консоль - должно быть "✅ Текстура загружена"
4. Если есть ошибка - читайте сообщение и исправляйте

## 📝 Пример структуры файлов

```
advanced/final-project-minecraft/
└── src/
    └── main/
        ├── java/
        │   └── com/minecraft/
        │       └── render/
        │           └── Texture.java      ← Класс загрузки (готов!)
        └── resources/
            └── textures/
                └── atlas.png             ← ВАША ТЕКСТУРА СЮДА!
```

## ✅ Готово!

После того как положите PNG файл:
1. Запустите игру
2. Проверьте консоль
3. Если всё ок - текстура загружена!
4. Теперь нужно добавить UV координаты в кубы (см. TEXTURES-GUIDE.md)

---

**Вопросы? Проверьте TEXTURES-GUIDE.md для подробностей!**
