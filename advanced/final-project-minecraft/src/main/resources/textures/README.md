# 🎨 Текстуры для Minecraft Clone

## 📁 Структура папки

```
textures/
├── blocks/
│   ├── grass_top.png       # Верх травы
│   ├── grass_side.png      # Бок травы
│   ├── dirt.png            # Земля
│   ├── stone.png           # Камень
│   ├── wood.png            # Дерево
│   ├── leaves.png          # Листва
│   ├── sand.png            # Песок
│   └── water.png           # Вода
├── atlas.png               # Атлас текстур (все в одном)
└── README.md               # Этот файл
```

## 🎨 Требования к текстурам

### Размер
- **Рекомендуемый:** 16x16 пикселей (классический Minecraft стиль)
- **Альтернативный:** 32x32 или 64x64 (HD текстуры)
- **Важно:** Все текстуры должны быть одного размера!

### Формат
- **PNG** с прозрачностью (для листвы, воды)
- **RGB** или **RGBA**

### Атлас текстур
Для оптимизации все текстуры объединяются в один файл (atlas.png):
```
┌────────┬────────┬────────┬────────┐
│ Grass  │ Dirt   │ Stone  │ Wood   │  16x16 каждая
│  Top   │        │        │        │
├────────┼────────┼────────┼────────┤
│ Grass  │ Leaves │ Sand   │ Water  │
│  Side  │        │        │        │
└────────┴────────┴────────┴────────┘
```
Размер атласа: 64x32 (4 текстуры в ширину, 2 в высоту)

## 🎯 Где взять текстуры

### 1. Создать самому
- Используйте Photoshop, GIMP, или Aseprite
- Pixel art стиль
- Простые цвета и формы

### 2. Бесплатные ресурсы
- **OpenGameArt.org** - бесплатные игровые ресурсы
- **itch.io** - много бесплатных asset pack'ов
- **Kenney.nl** - качественные бесплатные ассеты

### 3. Minecraft текстуры (для обучения)
⚠️ **Внимание:** Оригинальные текстуры Minecraft защищены авторским правом!
- Можно использовать для обучения
- Нельзя публиковать в коммерческих проектах
- Создайте свои уникальные текстуры для публикации

## 📝 Простые текстуры для начала

Если у вас нет текстур, можно использовать простые цвета:

### Grass (Трава)
- Верх: Зелёный (#7CBD6B)
- Бок: Зелёный верх + коричневый низ

### Dirt (Земля)
- Коричневый (#8B6F47)

### Stone (Камень)
- Серый (#7F7F7F)

### Wood (Дерево)
- Коричневый (#8B5A2B)

### Leaves (Листва)
- Тёмно-зелёный (#228B22)

### Sand (Песок)
- Жёлтый (#EDC9AF)

### Water (Вода)
- Синий (#1E90FF) с прозрачностью

## 🔧 Как использовать в коде

### Загрузка текстуры
```java
import org.lwjgl.stb.STBImage;
import static org.lwjgl.opengl.GL11.*;

public class Texture {
    private int textureId;
    
    public Texture(String path) {
        textureId = glGenTextures();
        glBindTexture(GL_TEXTURE_2D, textureId);
        
        // Загрузка через STB
        IntBuffer width = BufferUtils.createIntBuffer(1);
        IntBuffer height = BufferUtils.createIntBuffer(1);
        IntBuffer channels = BufferUtils.createIntBuffer(1);
        
        ByteBuffer image = STBImage.stbi_load(path, width, height, channels, 4);
        
        glTexImage2D(GL_TEXTURE_2D, 0, GL_RGBA, 
                     width.get(0), height.get(0), 
                     0, GL_RGBA, GL_UNSIGNED_BYTE, image);
        
        glTexParameteri(GL_TEXTURE_2D, GL_TEXTURE_MIN_FILTER, GL_NEAREST);
        glTexParameteri(GL_TEXTURE_2D, GL_TEXTURE_MAG_FILTER, GL_NEAREST);
        
        STBImage.stbi_image_free(image);
    }
}
```

### UV координаты для атласа
```java
// Для текстуры в позиции (x, y) в атласе 4x2
float u = x / 4.0f;      // 0.0, 0.25, 0.5, 0.75
float v = y / 2.0f;      // 0.0, 0.5
float uSize = 1.0f / 4.0f;  // 0.25
float vSize = 1.0f / 2.0f;  // 0.5

// UV координаты для одной грани
float[] uvs = {
    u,          v,           // Левый нижний
    u + uSize,  v,           // Правый нижний
    u + uSize,  v + vSize,   // Правый верхний
    u,          v + vSize    // Левый верхний
};
```

## 🎨 Создание простого атласа

### Вариант 1: Онлайн инструменты
- **Piskel** (piskelapp.com) - pixel art редактор
- **Photopea** (photopea.com) - бесплатный Photoshop онлайн

### Вариант 2: Программно
```java
// Создать атлас из отдельных текстур
BufferedImage atlas = new BufferedImage(64, 32, BufferedImage.TYPE_INT_ARGB);
Graphics2D g = atlas.createGraphics();

g.drawImage(grassTop, 0, 0, null);
g.drawImage(dirt, 16, 0, null);
g.drawImage(stone, 32, 0, null);
// ... и т.д.

ImageIO.write(atlas, "PNG", new File("atlas.png"));
```

## 📦 Пример структуры

Положите текстуры в:
```
src/main/resources/textures/
```

Загружайте в коде:
```java
Texture atlas = new Texture("textures/atlas.png");
```

## 🚀 Следующие шаги

1. Создайте или скачайте текстуры
2. Положите в `src/main/resources/textures/`
3. Создайте класс `Texture.java` для загрузки
4. Обновите `Renderer.java` для использования текстур
5. Добавьте UV координаты в вершины кубов

---

**Совет:** Начните с простых цветных квадратов, потом замените на красивые текстуры!
