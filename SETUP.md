# 🚀 Настройка сайта

## ⚙️ Конфигурация

### 1. Откройте `docs/script.js`

Найдите в начале файла:

```javascript
const GITHUB_USERNAME = 'YOUR_USERNAME'; // Замените на ваш username
const GITHUB_REPO = 'java-course'; // Замените на название репозитория
```

Замените на ваши данные:

```javascript
const GITHUB_USERNAME = 'your-github-username';
const GITHUB_REPO = 'java-course';
```

### 2. Обновите ссылки в `docs/index.html`

Найдите и замените `YOUR_USERNAME` на ваш GitHub username:

- В кнопке GitHub в навигации
- В ссылках на репозиторий
- В футере

## 🌐 Запуск локально

### Вариант 1: Python HTTP сервер

```bash
cd docs
python -m http.server 8000
```

Откройте: http://localhost:8000

### Вариант 2: VS Code Live Server

1. Установите расширение "Live Server"
2. Откройте `docs/index.html`
3. Нажмите "Go Live"

### Вариант 3: Node.js

```bash
cd docs
npx serve
```

## 📦 Публикация на GitHub Pages

### Автоматическая публикация

1. Push на GitHub:
```bash
git add .
git commit -m "🌐 Добавлен сайт с автоподтяжкой из GitHub"
git push origin main
```

2. Включите GitHub Pages:
   - Settings → Pages
   - Source: Deploy from a branch
   - Branch: `main`
   - Folder: `/docs`
   - Save

3. Сайт будет доступен через 1-2 минуты:
```
https://YOUR_USERNAME.github.io/java-course/
```

## ✨ Возможности сайта

### Автоматическая подтяжка данных:

1. **GitHub Stats** - автоматически загружаются:
   - ⭐ Количество звёзд
   - 🔄 Количество форков
   - 👁️ Количество watchers
   - 📝 Открытые Issues

2. **Модули курса** - автоматически загружаются из репозитория:
   - Beginner modules из папки `/beginner`
   - Intermediate modules из папки `/intermediate`
   - Advanced modules из папки `/advanced`

3. **Дата обновления** - автоматически из GitHub

### Интерактивные элементы:

- ✅ Плавная прокрутка
- ✅ Прогресс бар
- ✅ Анимированные счётчики
- ✅ Переключение вкладок модулей
- ✅ Активная навигация
- ✅ Мобильное меню
- ✅ Кнопка "Наверх"
- ✅ Анимации при скролле
- ✅ Частицы на фоне
- ✅ 3D кубы Minecraft
- ✅ Эффект печатания
- ✅ Hover эффекты

## 🎨 Кастомизация

### Цвета

Измените в `docs/styles.css`:

```css
:root {
    --primary: #6366f1;      /* Основной цвет */
    --secondary: #8b5cf6;    /* Вторичный */
    --accent: #ec4899;       /* Акцент */
}
```

### Контент

Измените в `docs/index.html`:
- Заголовки
- Описания
- Статистику
- Ссылки

## 🔧 Технологии

- **HTML5** - структура
- **CSS3** - стили, анимации, Grid, Flexbox
- **JavaScript** - интерактивность, GitHub API
- **GitHub API** - автоподтяжка данных
- **Intersection Observer API** - анимации при скролле

## 📊 GitHub API

Сайт использует GitHub API для автоматической подтяжки:

```javascript
// Статистика репозитория
GET https://api.github.com/repos/{username}/{repo}

// Содержимое папок
GET https://api.github.com/repos/{username}/{repo}/contents/{path}
```

**Лимиты:**
- Без авторизации: 60 запросов/час
- С авторизацией: 5000 запросов/час

Для большинства случаев 60 запросов достаточно.

## 🐛 Решение проблем

### GitHub Stats не загружаются

**Причина:** Неправильный username или название репозитория

**Решение:**
1. Проверьте `GITHUB_USERNAME` и `GITHUB_REPO` в `script.js`
2. Убедитесь что репозиторий публичный
3. Проверьте консоль браузера (F12)

### Модули не загружаются

**Причина:** Папки `beginner`, `intermediate`, `advanced` не найдены

**Решение:**
1. Убедитесь что папки существуют в репозитории
2. Проверьте названия папок (должны быть точно такие)
3. Если папок нет - отобразятся дефолтные модули

### Сайт не открывается локально

**Причина:** CORS ошибки

**Решение:** Используйте HTTP сервер (python, live-server, serve)

## 🚀 Производительность

Оптимизации:
- ✅ Минимальные зависимости (0 внешних библиотек)
- ✅ Оптимизированные анимации (CSS transforms)
- ✅ Lazy loading для GitHub API
- ✅ Кэширование данных
- ✅ Быстрая загрузка (<2 секунды)

## 📱 Адаптивность

Сайт полностью адаптивен:
- ✅ Десктоп (1200px+)
- ✅ Планшет (768px-1199px)
- ✅ Мобильный (<768px)

## 🎯 SEO

Добавьте в `<head>`:

```html
<meta name="description" content="Полный курс Java от новичка до профессионала">
<meta property="og:title" content="Курс Java">
<meta property="og:image" content="preview.png">
```

---

**Готово! Сайт настроен и готов к использованию! 🎉**

