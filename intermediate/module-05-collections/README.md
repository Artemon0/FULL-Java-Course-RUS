# Module 5: Коллекции и Generics

```
╔════════════════════════════════════════════════════════════╗
║  MODULE 5: КОЛЛЕКЦИИ И GENERICS                            ║
║  Уровень: Intermediate                                     ║
║  Время: 2-3 недели                                         ║
╚════════════════════════════════════════════════════════════╝
```

## 📖 Теория

### 5.1 Collections Framework

**Collections Framework** - набор интерфейсов и классов для работы с группами объектов.

```
                    Collection
                        │
        ┌───────────────┼───────────────┐
        │               │               │
      List            Set             Queue
        │               │               │
   ┌────┴────┐     ┌────┴────┐    ┌────┴────┐
   │         │     │         │    │         │
ArrayList  LinkedList  HashSet  TreeSet  PriorityQueue
Vector                 LinkedHashSet
```

```
                    Map
                     │
        ┌────────────┼────────────┐
        │            │            │
    HashMap      TreeMap    LinkedHashMap
  Hashtable
```

### 5.2 List - Упорядоченные коллекции

**List** - упорядоченная коллекция с доступом по индексу.

```
┌─────────────────────────────────────────────────┐
│  ArrayList                                      │
├─────────────────────────────────────────────────┤
│  + Быстрый доступ по индексу O(1)              │
│  - Медленная вставка/удаление O(n)             │
│  + Динамический размер                          │
│  Использование: когда нужен быстрый доступ     │
└─────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────┐
│  LinkedList                                     │
├─────────────────────────────────────────────────┤
│  - Медленный доступ по индексу O(n)            │
│  + Быстрая вставка/удаление O(1)               │
│  + Реализует Queue и Deque                      │
│  Использование: частые вставки/удаления        │
└─────────────────────────────────────────────────┘
```

### 5.3 Set - Уникальные элементы

**Set** - коллекция уникальных элементов (без дубликатов).

```
┌─────────────────────────────────────────────────┐
│  HashSet                                        │
├─────────────────────────────────────────────────┤
│  + Быстрые операции O(1)                       │
│  - Не гарантирует порядок                      │
│  + Не допускает дубликаты                      │
│  Использование: проверка наличия элемента      │
└─────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────┐
│  TreeSet                                        │
├─────────────────────────────────────────────────┤
│  + Отсортированные элементы                    │
│  - Медленнее HashSet O(log n)                  │
│  + Навигационные методы                        │
│  Использование: нужна сортировка               │
└─────────────────────────────────────────────────┘
```

### 5.4 Map - Пары ключ-значение

**Map** - коллекция пар ключ-значение.

```
┌─────────────────────────────────────────────────┐
│  HashMap                                        │
├─────────────────────────────────────────────────┤
│  + Быстрый доступ O(1)                         │
│  - Не гарантирует порядок                      │
│  + Один null ключ, много null значений         │
│  Использование: быстрый поиск по ключу         │
└─────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────┐
│  TreeMap                                        │
├─────────────────────────────────────────────────┤
│  + Отсортированные ключи                       │
│  - Медленнее HashMap O(log n)                  │
│  - Не допускает null ключи                     │
│  Использование: нужна сортировка ключей        │
└─────────────────────────────────────────────────┘
```

### 5.5 Generics (Обобщения)

**Generics** - параметризация типов для типобезопасности.

```java
// Без Generics (старый способ)
List list = new ArrayList();
list.add("String");
list.add(123);
String s = (String) list.get(0);  // Нужно приведение типа

// С Generics
List<String> list = new ArrayList<>();
list.add("String");
// list.add(123);  // ОШИБКА КОМПИЛЯЦИИ!
String s = list.get(0);  // Приведение не нужно
```

**Синтаксис:**
```java
class Box<T> {
    private T value;
    
    public void set(T value) {
        this.value = value;
    }
    
    public T get() {
        return value;
    }
}
```

### 5.6 Comparable и Comparator

```java
// Comparable - естественный порядок сортировки
class Person implements Comparable<Person> {
    @Override
    public int compareTo(Person other) {
        return this.age - other.age;
    }
}

// Comparator - пользовательский порядок
Comparator<Person> byName = (p1, p2) -> p1.name.compareTo(p2.name);
```

---

## 💻 Примеры кода

### Пример 1: ArrayList
```java
import java.util.ArrayList;
import java.util.List;

public class ArrayListExample {
    public static void main(String[] args) {
        // Создание ArrayList
        List<String> fruits = new ArrayList<>();
        
        // Добавление элементов
        fruits.add("Яблоко");
        fruits.add("Банан");
        fruits.add("Апельсин");
        
        // Доступ по индексу
        System.out.println("Первый фрукт: " + fruits.get(0));
        
        // Размер
        System.out.println("Количество: " + fruits.size());
        
        // Проверка наличия
        System.out.println("Есть банан? " + fruits.contains("Банан"));
        
        // Удаление
        fruits.remove("Банан");
        fruits.remove(0);  // по индексу
        
        // Итерация
        for (String fruit : fruits) {
            System.out.println(fruit);
        }
        
        // Очистка
        fruits.clear();
        System.out.println("Пустой? " + fruits.isEmpty());
    }
}
```

### Пример 2: LinkedList
```java
import java.util.LinkedList;

public class LinkedListExample {
    public static void main(String[] args) {
        LinkedList<Integer> numbers = new LinkedList<>();
        
        // Добавление в начало и конец
        numbers.addFirst(1);
        numbers.addLast(3);
        numbers.add(1, 2);  // вставка по индексу
        
        System.out.println(numbers);  // [1, 2, 3]
        
        // Получение первого и последнего
        System.out.println("Первый: " + numbers.getFirst());
        System.out.println("Последний: " + numbers.getLast());
        
        // Удаление первого и последнего
        numbers.removeFirst();
        numbers.removeLast();
        
        // Использование как очередь
        numbers.offer(10);  // добавить в конец
        numbers.offer(20);
        System.out.println("Извлечь: " + numbers.poll());  // извлечь из начала
        
        // Использование как стек
        numbers.push(30);  // добавить в начало
        System.out.println("Pop: " + numbers.pop());  // извлечь из начала
    }
}
```

### Пример 3: HashSet
```java
import java.util.HashSet;
import java.util.Set;

public class HashSetExample {
    public static void main(String[] args) {
        Set<String> uniqueNames = new HashSet<>();
        
        // Добавление элементов
        uniqueNames.add("Иван");
        uniqueNames.add("Мария");
        uniqueNames.add("Иван");  // дубликат - не добавится
        
        System.out.println("Размер: " + uniqueNames.size());  // 2
        
        // Проверка наличия
        if (uniqueNames.contains("Иван")) {
            System.out.println("Иван в множестве");
        }
        
        // Итерация (порядок не гарантирован)
        for (String name : uniqueNames) {
            System.out.println(name);
        }
        
        // Удаление дубликатов из списка
        List<Integer> numbersWithDuplicates = Arrays.asList(1, 2, 2, 3, 3, 3, 4);
        Set<Integer> uniqueNumbers = new HashSet<>(numbersWithDuplicates);
        System.out.println("Уникальные: " + uniqueNumbers);
    }
}
```

### Пример 4: TreeSet
```java
import java.util.TreeSet;

public class TreeSetExample {
    public static void main(String[] args) {
        TreeSet<Integer> numbers = new TreeSet<>();
        
        // Добавление (автоматическая сортировка)
        numbers.add(5);
        numbers.add(1);
        numbers.add(3);
        numbers.add(2);
        
        System.out.println(numbers);  // [1, 2, 3, 5] - отсортировано!
        
        // Навигационные методы
        System.out.println("Первый: " + numbers.first());
        System.out.println("Последний: " + numbers.last());
        System.out.println("Меньше 3: " + numbers.headSet(3));
        System.out.println("Больше или равно 3: " + numbers.tailSet(3));
        System.out.println("От 2 до 5: " + numbers.subSet(2, 5));
        
        // Ближайшие элементы
        System.out.println("Меньше или равно 4: " + numbers.floor(4));
        System.out.println("Больше или равно 4: " + numbers.ceiling(4));
    }
}
```

### Пример 5: HashMap
```java
import java.util.HashMap;
import java.util.Map;

public class HashMapExample {
    public static void main(String[] args) {
        Map<String, Integer> ages = new HashMap<>();
        
        // Добавление пар ключ-значение
        ages.put("Иван", 25);
        ages.put("Мария", 30);
        ages.put("Петр", 28);
        
        // Получение значения по ключу
        System.out.println("Возраст Ивана: " + ages.get("Иван"));
        
        // Проверка наличия ключа
        if (ages.containsKey("Мария")) {
            System.out.println("Мария найдена");
        }
        
        // Проверка наличия значения
        if (ages.containsValue(30)) {
            System.out.println("Кому-то 30 лет");
        }
        
        // Обновление значения
        ages.put("Иван", 26);  // перезапись
        
        // Получение с значением по умолчанию
        int age = ages.getOrDefault("Анна", 0);
        
        // Итерация по ключам
        for (String name : ages.keySet()) {
            System.out.println(name);
        }
        
        // Итерация по значениям
        for (Integer ageValue : ages.values()) {
            System.out.println(ageValue);
        }
        
        // Итерация по парам
        for (Map.Entry<String, Integer> entry : ages.entrySet()) {
            System.out.println(entry.getKey() + ": " + entry.getValue());
        }
        
        // Удаление
        ages.remove("Петр");
    }
}
```

### Пример 6: Generics - создание обобщённого класса
```java
public class Box<T> {
    private T content;
    
    public void put(T item) {
        this.content = item;
    }
    
    public T get() {
        return content;
    }
    
    public boolean isEmpty() {
        return content == null;
    }
}

// Использование
public class Main {
    public static void main(String[] args) {
        // Box для строк
        Box<String> stringBox = new Box<>();
        stringBox.put("Hello");
        String s = stringBox.get();
        
        // Box для чисел
        Box<Integer> intBox = new Box<>();
        intBox.put(123);
        int num = intBox.get();
        
        // Box для любых объектов
        Box<Person> personBox = new Box<>();
        personBox.put(new Person("Иван", 25));
    }
}
```

### Пример 7: Generics - обобщённые методы
```java
public class GenericMethods {
    // Обобщённый метод для вывода массива
    public static <T> void printArray(T[] array) {
        for (T element : array) {
            System.out.print(element + " ");
        }
        System.out.println();
    }
    
    // Обобщённый метод для поиска максимума
    public static <T extends Comparable<T>> T findMax(T[] array) {
        if (array == null || array.length == 0) {
            return null;
        }
        
        T max = array[0];
        for (int i = 1; i < array.length; i++) {
            if (array[i].compareTo(max) > 0) {
                max = array[i];
            }
        }
        return max;
    }
    
    public static void main(String[] args) {
        Integer[] numbers = {1, 5, 3, 9, 2};
        String[] words = {"apple", "banana", "cherry"};
        
        printArray(numbers);
        printArray(words);
        
        System.out.println("Max number: " + findMax(numbers));
        System.out.println("Max word: " + findMax(words));
    }
}
```

### Пример 8: Comparable
```java
public class Student implements Comparable<Student> {
    private String name;
    private double gpa;
    
    public Student(String name, double gpa) {
        this.name = name;
        this.gpa = gpa;
    }
    
    @Override
    public int compareTo(Student other) {
        // Сортировка по GPA (по убыванию)
        return Double.compare(other.gpa, this.gpa);
    }
    
    @Override
    public String toString() {
        return name + " (GPA: " + gpa + ")";
    }
}

// Использование
public class Main {
    public static void main(String[] args) {
        List<Student> students = new ArrayList<>();
        students.add(new Student("Иван", 4.5));
        students.add(new Student("Мария", 4.8));
        students.add(new Student("Петр", 4.2));
        
        Collections.sort(students);  // Использует compareTo
        
        for (Student student : students) {
            System.out.println(student);
        }
    }
}
```

### Пример 9: Comparator
```java
import java.util.Comparator;

public class ComparatorExample {
    public static void main(String[] args) {
        List<Person> people = new ArrayList<>();
        people.add(new Person("Иван", 25));
        people.add(new Person("Мария", 30));
        people.add(new Person("Петр", 20));
        
        // Сортировка по возрасту
        Collections.sort(people, new Comparator<Person>() {
            @Override
            public int compare(Person p1, Person p2) {
                return p1.getAge() - p2.getAge();
            }
        });
        
        // Или с лямбда-выражением
        Collections.sort(people, (p1, p2) -> p1.getAge() - p2.getAge());
        
        // Или ещё проще
        people.sort(Comparator.comparingInt(Person::getAge));
        
        // Сортировка по имени
        people.sort(Comparator.comparing(Person::getName));
        
        // Сложная сортировка (по возрасту, затем по имени)
        people.sort(Comparator.comparingInt(Person::getAge)
                             .thenComparing(Person::getName));
        
        // Обратная сортировка
        people.sort(Comparator.comparingInt(Person::getAge).reversed());
    }
}
```

### Пример 10: Collections Utility Class
```java
import java.util.Collections;

public class CollectionsUtility {
    public static void main(String[] args) {
        List<Integer> numbers = new ArrayList<>(Arrays.asList(5, 2, 8, 1, 9));
        
        // Сортировка
        Collections.sort(numbers);
        System.out.println("Отсортировано: " + numbers);
        
        // Обратная сортировка
        Collections.reverse(numbers);
        System.out.println("Обратный порядок: " + numbers);
        
        // Перемешивание
        Collections.shuffle(numbers);
        System.out.println("Перемешано: " + numbers);
        
        // Поиск (список должен быть отсортирован)
        Collections.sort(numbers);
        int index = Collections.binarySearch(numbers, 5);
        System.out.println("Индекс 5: " + index);
        
        // Максимум и минимум
        System.out.println("Максимум: " + Collections.max(numbers));
        System.out.println("Минимум: " + Collections.min(numbers));
        
        // Частота элемента
        numbers.add(5);
        numbers.add(5);
        System.out.println("Частота 5: " + Collections.frequency(numbers, 5));
        
        // Заполнение
        Collections.fill(numbers, 0);
        System.out.println("Заполнено нулями: " + numbers);
        
        // Неизменяемая коллекция
        List<String> immutableList = Collections.unmodifiableList(
            Arrays.asList("a", "b", "c")
        );
        // immutableList.add("d");  // UnsupportedOperationException
    }
}
```

---

## ✏️ Мини-задания

### Задание 1: Удаление дубликатов
Напишите метод, который удаляет дубликаты из ArrayList.

### Задание 2: Частота слов
Подсчитайте частоту каждого слова в тексте, используя HashMap.

### Задание 3: Топ-N элементов
Найдите N самых больших элементов в списке.

### Задание 4: Пересечение множеств
Найдите общие элементы двух множеств.

### Задание 5: Обратный словарь
Создайте Map, где ключи и значения поменяны местами.

---

## 🔨 Практические упражнения

### Упражнение 1: Телефонная книга
Создайте телефонную книгу с использованием HashMap:
- Добавление контакта
- Поиск по имени
- Удаление контакта
- Вывод всех контактов (отсортированных)

### Упражнение 2: Система оценок
Создайте систему для хранения оценок студентов:
- Map<Student, List<Integer>>
- Добавление оценок
- Вычисление среднего балла
- Поиск лучших студентов

### Упражнение 3: Анализ текста
Напишите программу для анализа текста:
- Подсчёт уникальных слов
- Самые частые слова
- Слова определённой длины
- Статистика по буквам

### Упражнение 4: Менеджер задач
Создайте менеджер задач с приоритетами:
- Используйте PriorityQueue
- Добавление задач с приоритетом
- Выполнение задач по приоритету
- Фильтрация задач

### Упражнение 5: Граф (социальная сеть)
Реализуйте простой граф для социальной сети:
- Map<Person, Set<Person>> для связей
- Добавление друзей
- Поиск общих друзей
- Рекомендации друзей

---

## 🎨 Мини-проект: Система управления библиотекой v2.0

Улучшите проект библиотеки из Beginner уровня, используя коллекции.

**Требования:**

1. **Используйте правильные коллекции:**
   - `List<Book>` для списка книг
   - `Map<String, Book>` для быстрого поиска по ISBN
   - `Set<String>` для уникальных жанров
   - `Map<Member, List<Book>>` для истории займов

2. **Функционал:**
   - Добавление/удаление книг
   - Поиск книг (по названию, автору, жанру)
   - Сортировка книг (по году, рейтингу, названию)
   - Статистика (самые популярные книги, жанры)
   - Рекомендации на основе истории

3. **Дополнительно:**
   - Создайте обобщённый класс `Catalog<T>`
   - Реализуйте Comparable для Book
   - Используйте Comparator для разных видов сортировки

---

## ➡️ Следующий модуль

После завершения переходите к [Module 6: Stream API](../module-06-streams/README.md)
