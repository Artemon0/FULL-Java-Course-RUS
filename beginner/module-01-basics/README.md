# Module 1: Основы Java

```
╔════════════════════════════════════════════════════════════╗
║  MODULE 1: ОСНОВЫ JAVA                                     ║
║  Уровень: Beginner                                         ║
║  Время: 1-2 недели                                         ║
╚════════════════════════════════════════════════════════════╝
```

## 📖 Теория

### 1.1 Что такое Java?

Java - это объектно-ориентированный язык программирования, созданный в 1995 году компанией Sun Microsystems (сейчас Oracle).

**Ключевые особенности:**
- ☕ Платформонезависимость ("Write Once, Run Anywhere")
- 🔒 Строгая типизация
- 🗑️ Автоматическое управление памятью (Garbage Collection)
- 🧵 Встроенная поддержка многопоточности
- 📚 Огромная экосистема библиотек

### 1.2 JDK, JRE, JVM - в чём разница?

```
┌─────────────────────────────────────────┐
│              JDK                        │
│  (Java Development Kit)                 │
│  ┌───────────────────────────────────┐  │
│  │         JRE                       │  │
│  │  (Java Runtime Environment)       │  │
│  │  ┌─────────────────────────────┐  │  │
│  │  │       JVM                   │  │  │
│  │  │  (Java Virtual Machine)     │  │  │
│  │  │                             │  │  │
│  │  │  - Выполняет байт-код       │  │  │
│  │  │  - Управляет памятью        │  │  │
│  │  └─────────────────────────────┘  │  │
│  │                                   │  │
│  │  + Библиотеки классов             │  │
│  └───────────────────────────────────┘  │
│                                         │
│  + Компилятор (javac)                   │
│  + Отладчик (jdb)                       │
│  + Документация (javadoc)               │
└─────────────────────────────────────────┘
```

**JVM (Java Virtual Machine):**
- Виртуальная машина, которая выполняет байт-код
- Обеспечивает платформонезависимость

**JRE (Java Runtime Environment):**
- JVM + стандартные библиотеки
- Нужна для запуска Java-приложений

**JDK (Java Development Kit):**
- JRE + инструменты разработки
- Нужна для создания Java-приложений

### 1.3 Процесс компиляции и выполнения

```
┌──────────────┐      ┌──────────────┐      ┌──────────────┐
│  Hello.java  │─────►│ Hello.class  │─────►│     JVM      │
│              │      │              │      │              │
│ (исходный    │ javac│ (байт-код)   │ java │ (выполнение) │
│  код)        │      │              │      │              │
└──────────────┘      └──────────────┘      └──────────────┘
   Компиляция            Загрузка           Интерпретация
```

### 1.4 Установка JDK

1. Скачайте JDK (рекомендуется версия 17 или 21 LTS)
2. Установите JDK
3. Настройте переменную окружения `JAVA_HOME`
4. Добавьте `%JAVA_HOME%\bin` в `PATH`

**Проверка установки:**
```bash
java -version
javac -version
```

### 1.5 Первая программа

Структура простейшей программы:

```java
public class HelloWorld {
    public static void main(String[] args) {
        System.out.println("Hello, World!");
    }
}
```

**Разбор по частям:**
- `public class HelloWorld` - объявление публичного класса
- `public static void main(String[] args)` - точка входа в программу
- `System.out.println()` - вывод текста в консоль

---

## 💻 Примеры кода

### Пример 1: Hello World
```java
public class HelloWorld {
    public static void main(String[] args) {
        System.out.println("Hello, World!");
    }
}
```

### Пример 2: Вывод нескольких строк
```java
public class MultipleLines {
    public static void main(String[] args) {
        System.out.println("Добро пожаловать в Java!");
        System.out.println("Это вторая строка");
        System.out.println("Это третья строка");
    }
}
```

### Пример 3: Комментарии
```java
public class Comments {
    public static void main(String[] args) {
        // Это однострочный комментарий
        System.out.println("Привет!"); // Комментарий в конце строки
        
        /*
         * Это многострочный
         * комментарий
         */
        System.out.println("Мир!");
        
        /**
         * Это JavaDoc комментарий
         * Используется для документации
         */
    }
}
```

### Пример 4: Переменные и типы данных
```java
public class Variables {
    public static void main(String[] args) {
        // Целые числа
        int age = 25;
        long population = 7800000000L;
        
        // Дробные числа
        double price = 19.99;
        float temperature = 36.6f;
        
        // Символы и строки
        char grade = 'A';
        String name = "Иван";
        
        // Логический тип
        boolean isStudent = true;
        
        System.out.println("Имя: " + name);
        System.out.println("Возраст: " + age);
        System.out.println("Студент: " + isStudent);
    }
}
```

### Пример 5: Арифметические операции
```java
public class Arithmetic {
    public static void main(String[] args) {
        int a = 10;
        int b = 3;
        
        System.out.println("Сложение: " + (a + b));      // 13
        System.out.println("Вычитание: " + (a - b));     // 7
        System.out.println("Умножение: " + (a * b));     // 30
        System.out.println("Деление: " + (a / b));       // 3
        System.out.println("Остаток: " + (a % b));       // 1
        
        double c = 10.0;
        double d = 3.0;
        System.out.println("Деление double: " + (c / d)); // 3.333...
    }
}
```

### Пример 6: Ввод данных от пользователя
```java
import java.util.Scanner;

public class UserInput {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        
        System.out.print("Введите ваше имя: ");
        String name = scanner.nextLine();
        
        System.out.print("Введите ваш возраст: ");
        int age = scanner.nextInt();
        
        System.out.println("Привет, " + name + "!");
        System.out.println("Вам " + age + " лет.");
        
        scanner.close();
    }
}
```

### Пример 7: Константы
```java
public class Constants {
    public static void main(String[] args) {
        final double PI = 3.14159;
        final int MAX_USERS = 100;
        
        double radius = 5.0;
        double area = PI * radius * radius;
        
        System.out.println("Площадь круга: " + area);
        System.out.println("Максимум пользователей: " + MAX_USERS);
        
        // PI = 3.14; // ОШИБКА! Константу нельзя изменить
    }
}
```

### Пример 8: Приведение типов
```java
public class TypeCasting {
    public static void main(String[] args) {
        // Автоматическое приведение (расширение)
        int intValue = 100;
        long longValue = intValue;
        double doubleValue = intValue;
        
        System.out.println("int -> long: " + longValue);
        System.out.println("int -> double: " + doubleValue);
        
        // Явное приведение (сужение)
        double pi = 3.14159;
        int piInt = (int) pi;  // Дробная часть отбрасывается
        
        System.out.println("double -> int: " + piInt);
        
        // Приведение в вычислениях
        int a = 5;
        int b = 2;
        double result = (double) a / b;
        
        System.out.println("5 / 2 = " + result);
    }
}
```

### Пример 9: Строки и конкатенация
```java
public class Strings {
    public static void main(String[] args) {
        String firstName = "Иван";
        String lastName = "Иванов";
        
        // Конкатенация
        String fullName = firstName + " " + lastName;
        System.out.println(fullName);
        
        // Методы строк
        System.out.println("Длина: " + fullName.length());
        System.out.println("Верхний регистр: " + fullName.toUpperCase());
        System.out.println("Нижний регистр: " + fullName.toLowerCase());
        System.out.println("Содержит 'Иван': " + fullName.contains("Иван"));
        
        // Форматирование
        int age = 25;
        String message = String.format("%s, вам %d лет", fullName, age);
        System.out.println(message);
    }
}
```

### Пример 10: Базовая математика
```java
public class MathOperations {
    public static void main(String[] args) {
        // Класс Math
        System.out.println("Абсолютное значение: " + Math.abs(-10));
        System.out.println("Максимум: " + Math.max(5, 10));
        System.out.println("Минимум: " + Math.min(5, 10));
        System.out.println("Степень: " + Math.pow(2, 3));
        System.out.println("Квадратный корень: " + Math.sqrt(16));
        System.out.println("Округление вверх: " + Math.ceil(4.3));
        System.out.println("Округление вниз: " + Math.floor(4.7));
        System.out.println("Округление: " + Math.round(4.5));
        System.out.println("Случайное число: " + Math.random());
    }
}
```

---

## ✏️ Мини-задания

### Задание 1: Приветствие
Создайте программу, которая выводит ваше имя и любимый язык программирования.

### Задание 2: Калькулятор возраста
Создайте программу, которая вычисляет, сколько вам будет лет через 10 лет.

### Задание 3: Конвертер температуры
Напишите программу, которая конвертирует температуру из Цельсия в Фаренгейт.
Формула: F = C × 9/5 + 32

### Задание 4: Площадь прямоугольника
Запросите у пользователя длину и ширину, вычислите площадь и периметр.

### Задание 5: Обмен значений
Создайте две переменные и поменяйте их значения местами (используя третью переменную).

---

## 🔨 Практические упражнения

### Упражнение 1: Визитная карточка
Создайте программу, которая выводит вашу "визитную карточку":
- Имя
- Возраст
- Город
- Профессия/Учёба
- Хобби

Используйте красивое форматирование с рамками из символов.

### Упражнение 2: Калькулятор покупок
Создайте программу для расчёта покупок:
- Запросите цену товара
- Запросите количество
- Запросите процент скидки
- Вычислите итоговую стоимость
- Выведите детальный чек

### Упражнение 3: Конвертер валют
Создайте конвертер валют:
- Запросите сумму в рублях
- Конвертируйте в доллары, евро и юани
- Используйте константы для курсов валют

### Упражнение 4: Вычисление ИМТ
Создайте калькулятор индекса массы тела:
- Запросите вес (кг) и рост (м)
- Вычислите ИМТ = вес / (рост²)
- Выведите результат

### Упражнение 5: Секунды в время
Запросите количество секунд и конвертируйте в часы, минуты и секунды.
Например: 3665 секунд = 1 час 1 минута 5 секунд

---

## 🎨 Мини-проект: Калькулятор путешествия

Создайте программу для планирования путешествия:

**Требования:**
1. Запросите у пользователя:
   - Расстояние до места назначения (км)
   - Средняя скорость (км/ч)
   - Расход топлива (л/100км)
   - Цена топлива (руб/л)
   - Количество пассажиров

2. Вычислите и выведите:
   - Время в пути (часы и минуты)
   - Общий расход топлива
   - Стоимость топлива
   - Стоимость на одного пассажира

3. Добавьте красивое форматирование вывода

**Пример вывода:**
```
╔════════════════════════════════════════╗
║     КАЛЬКУЛЯТОР ПУТЕШЕСТВИЯ            ║
╚════════════════════════════════════════╝

Расстояние: 450 км
Скорость: 90 км/ч
Расход: 7.5 л/100км
Цена топлива: 55 руб/л
Пассажиров: 3

────────────────────────────────────────
РЕЗУЛЬТАТЫ:
────────────────────────────────────────
Время в пути: 5 ч 0 мин
Расход топлива: 33.75 л
Стоимость топлива: 1856.25 руб
На человека: 618.75 руб
```

---

## 📝 Решения

Решения всех заданий находятся в папке `solutions/`.
Попробуйте сначала решить самостоятельно!

---

## ➡️ Следующий модуль

После завершения всех заданий переходите к [Module 2: Синтаксис и управление потоком](../module-02-syntax/README.md)
