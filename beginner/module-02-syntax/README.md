# Module 2: Синтаксис и управление потоком

```
╔════════════════════════════════════════════════════════════╗
║  MODULE 2: СИНТАКСИС И УПРАВЛЕНИЕ ПОТОКОМ                  ║
║  Уровень: Beginner                                         ║
║  Время: 1-2 недели                                         ║
╚════════════════════════════════════════════════════════════╝
```

## 📖 Теория

### 2.1 Условные операторы

#### if-else

```
┌─────────────┐
│  Условие?   │
└──────┬──────┘
       │
   ┌───┴───┐
   │       │
  Да      Нет
   │       │
   ▼       ▼
┌─────┐ ┌─────┐
│ if  │ │else │
└─────┘ └─────┘
```

**Синтаксис:**
```java
if (условие) {
    // код, если условие истинно
} else if (другое_условие) {
    // код для другого условия
} else {
    // код, если все условия ложны
}
```

#### Операторы сравнения
```
==  равно
!=  не равно
>   больше
<   меньше
>=  больше или равно
<=  меньше или равно
```

#### Логические операторы
```
&&  И (AND)   - оба условия должны быть истинны
||  ИЛИ (OR)  - хотя бы одно условие истинно
!   НЕ (NOT)  - инверсия условия
```

#### Тернарный оператор
```java
результат = (условие) ? значение_если_true : значение_если_false;
```

### 2.2 Switch-case

```java
switch (переменная) {
    case значение1:
        // код
        break;
    case значение2:
        // код
        break;
    default:
        // код по умолчанию
}
```

**Switch Expressions (Java 14+):**
```java
String result = switch (day) {
    case 1, 2, 3, 4, 5 -> "Будний день";
    case 6, 7 -> "Выходной";
    default -> "Неверный день";
};
```

### 2.3 Циклы

#### Цикл while
```
┌──────────────┐
│  Условие?    │◄─────┐
└──────┬───────┘      │
       │              │
      Да              │
       │              │
       ▼              │
   ┌───────┐          │
   │  Тело │──────────┘
   │ цикла │
   └───────┘
       │
      Нет
       ▼
```

```java
while (условие) {
    // код выполняется, пока условие истинно
}
```

#### Цикл do-while
```java
do {
    // код выполнится минимум один раз
} while (условие);
```

#### Цикл for
```java
for (инициализация; условие; инкремент) {
    // тело цикла
}
```

#### Цикл for-each
```java
for (тип элемент : коллекция) {
    // обработка элемента
}
```

### 2.4 Управление циклами

```
break    - выход из цикла
continue - переход к следующей итерации
return   - выход из метода
```

---

## 💻 Примеры кода

### Пример 1: Простые условия
```java
public class SimpleConditions {
    public static void main(String[] args) {
        int age = 18;
        
        if (age >= 18) {
            System.out.println("Вы совершеннолетний");
        } else {
            System.out.println("Вы несовершеннолетний");
        }
        
        // Тернарный оператор
        String status = (age >= 18) ? "Взрослый" : "Ребёнок";
        System.out.println("Статус: " + status);
    }
}
```

### Пример 2: Множественные условия
```java
public class MultipleConditions {
    public static void main(String[] args) {
        int score = 85;
        
        if (score >= 90) {
            System.out.println("Оценка: Отлично");
        } else if (score >= 75) {
            System.out.println("Оценка: Хорошо");
        } else if (score >= 60) {
            System.out.println("Оценка: Удовлетворительно");
        } else {
            System.out.println("Оценка: Неудовлетворительно");
        }
    }
}
```

### Пример 3: Логические операторы
```java
public class LogicalOperators {
    public static void main(String[] args) {
        int age = 25;
        boolean hasLicense = true;
        boolean hasCar = false;
        
        // И (AND)
        if (age >= 18 && hasLicense) {
            System.out.println("Можете водить машину");
        }
        
        // ИЛИ (OR)
        if (hasCar || hasLicense) {
            System.out.println("Есть связь с автомобилями");
        }
        
        // НЕ (NOT)
        if (!hasCar) {
            System.out.println("Нет машины");
        }
        
        // Комбинация
        if ((age >= 18 && hasLicense) || hasCar) {
            System.out.println("Сложное условие выполнено");
        }
    }
}
```

### Пример 4: Switch-case
```java
public class SwitchExample {
    public static void main(String[] args) {
        int dayOfWeek = 3;
        String dayName;
        
        switch (dayOfWeek) {
            case 1:
                dayName = "Понедельник";
                break;
            case 2:
                dayName = "Вторник";
                break;
            case 3:
                dayName = "Среда";
                break;
            case 4:
                dayName = "Четверг";
                break;
            case 5:
                dayName = "Пятница";
                break;
            case 6:
                dayName = "Суббота";
                break;
            case 7:
                dayName = "Воскресенье";
                break;
            default:
                dayName = "Неверный день";
        }
        
        System.out.println("День недели: " + dayName);
    }
}
```

### Пример 5: Цикл while
```java
public class WhileLoop {
    public static void main(String[] args) {
        // Обратный отсчёт
        int count = 5;
        
        while (count > 0) {
            System.out.println("Осталось: " + count);
            count--;
        }
        
        System.out.println("Пуск!");
        
        // Сумма чисел
        int sum = 0;
        int i = 1;
        
        while (i <= 10) {
            sum += i;
            i++;
        }
        
        System.out.println("Сумма чисел от 1 до 10: " + sum);
    }
}
```

### Пример 6: Цикл do-while
```java
import java.util.Scanner;

public class DoWhileLoop {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        int number;
        
        do {
            System.out.print("Введите положительное число: ");
            number = scanner.nextInt();
            
            if (number <= 0) {
                System.out.println("Ошибка! Число должно быть положительным.");
            }
        } while (number <= 0);
        
        System.out.println("Вы ввели: " + number);
        scanner.close();
    }
}
```

### Пример 7: Цикл for
```java
public class ForLoop {
    public static void main(String[] args) {
        // Простой цикл
        for (int i = 1; i <= 5; i++) {
            System.out.println("Итерация " + i);
        }
        
        // Таблица умножения
        int number = 7;
        System.out.println("\nТаблица умножения на " + number + ":");
        
        for (int i = 1; i <= 10; i++) {
            System.out.println(number + " × " + i + " = " + (number * i));
        }
        
        // Чётные числа
        System.out.println("\nЧётные числа от 2 до 20:");
        for (int i = 2; i <= 20; i += 2) {
            System.out.print(i + " ");
        }
    }
}
```

### Пример 8: Вложенные циклы
```java
public class NestedLoops {
    public static void main(String[] args) {
        // Таблица умножения
        System.out.println("Таблица умножения:");
        
        for (int i = 1; i <= 5; i++) {
            for (int j = 1; j <= 5; j++) {
                System.out.printf("%4d", i * j);
            }
            System.out.println();
        }
        
        // Треугольник из звёздочек
        System.out.println("\nТреугольник:");
        
        for (int i = 1; i <= 5; i++) {
            for (int j = 1; j <= i; j++) {
                System.out.print("* ");
            }
            System.out.println();
        }
    }
}
```

### Пример 9: Break и Continue
```java
public class BreakContinue {
    public static void main(String[] args) {
        // Break - выход из цикла
        System.out.println("Поиск первого числа, делящегося на 7:");
        for (int i = 1; i <= 100; i++) {
            if (i % 7 == 0) {
                System.out.println("Найдено: " + i);
                break;
            }
        }
        
        // Continue - пропуск итерации
        System.out.println("\nЧисла от 1 до 10, кроме 5:");
        for (int i = 1; i <= 10; i++) {
            if (i == 5) {
                continue;
            }
            System.out.print(i + " ");
        }
        
        // Пропуск чётных чисел
        System.out.println("\n\nНечётные числа от 1 до 20:");
        for (int i = 1; i <= 20; i++) {
            if (i % 2 == 0) {
                continue;
            }
            System.out.print(i + " ");
        }
    }
}
```

### Пример 10: Комплексный пример
```java
import java.util.Scanner;

public class NumberGuessing {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        int secretNumber = 42;
        int attempts = 0;
        int maxAttempts = 5;
        boolean guessed = false;
        
        System.out.println("Игра 'Угадай число'");
        System.out.println("Я загадал число от 1 до 100");
        System.out.println("У вас " + maxAttempts + " попыток\n");
        
        while (attempts < maxAttempts && !guessed) {
            System.out.print("Попытка " + (attempts + 1) + ": ");
            int guess = scanner.nextInt();
            attempts++;
            
            if (guess == secretNumber) {
                guessed = true;
                System.out.println("Поздравляю! Вы угадали!");
                System.out.println("Попыток использовано: " + attempts);
            } else if (guess < secretNumber) {
                System.out.println("Загаданное число больше");
            } else {
                System.out.println("Загаданное число меньше");
            }
            
            if (!guessed && attempts < maxAttempts) {
                System.out.println("Осталось попыток: " + (maxAttempts - attempts) + "\n");
            }
        }
        
        if (!guessed) {
            System.out.println("\nВы проиграли! Загаданное число: " + secretNumber);
        }
        
        scanner.close();
    }
}
```

---

## ✏️ Мини-задания

### Задание 1: Проверка чётности
Напишите программу, которая проверяет, является ли число чётным или нечётным.

### Задание 2: Максимум из трёх
Найдите максимальное из трёх введённых чисел.

### Задание 3: Калькулятор
Создайте простой калькулятор с использованием switch для выбора операции (+, -, *, /).

### Задание 4: Сумма чисел
Вычислите сумму всех чисел от 1 до 100.

### Задание 5: Факториал
Вычислите факториал числа (n! = 1 × 2 × 3 × ... × n).

---

## 🔨 Практические упражнения

### Упражнение 1: Определение сезона
Создайте программу, которая по номеру месяца определяет сезон:
- 12, 1, 2 - Зима
- 3, 4, 5 - Весна
- 6, 7, 8 - Лето
- 9, 10, 11 - Осень

### Упражнение 2: Проверка високосного года
Год является високосным, если:
- Делится на 4 И не делится на 100
- ИЛИ делится на 400

### Упражнение 3: Таблица степеней
Выведите таблицу степеней числа 2 от 2⁰ до 2¹⁰.

### Упражнение 4: Простые числа
Найдите все простые числа от 1 до 100.

### Упражнение 5: Пирамида
Нарисуйте пирамиду из звёздочек:
```
    *
   ***
  *****
 *******
*********
```

---

## 🎨 Мини-проект: Меню ресторана

Создайте интерактивное меню ресторана с возможностью заказа.

**Требования:**

1. Меню с категориями:
   - Закуски
   - Основные блюда
   - Десерты
   - Напитки

2. Функционал:
   - Показать меню
   - Добавить блюдо в заказ
   - Показать текущий заказ
   - Вычислить итоговую сумму
   - Применить скидку (если сумма > 1000 руб)
   - Завершить заказ

3. Используйте:
   - Switch для навигации по меню
   - Циклы для повторного выбора
   - Условия для применения скидок

**Пример работы:**
```
╔════════════════════════════════════════╗
║        РЕСТОРАН "JAVA CAFE"            ║
╚════════════════════════════════════════╝

1. Показать меню
2. Добавить блюдо
3. Мой заказ
4. Оформить заказ
0. Выход

Ваш выбор: 1

═══ МЕНЮ ═══
ЗАКУСКИ:
1. Салат Цезарь - 350 руб
2. Брускетта - 250 руб

ОСНОВНЫЕ БЛЮДА:
3. Стейк - 890 руб
4. Паста Карбонара - 450 руб

ДЕСЕРТЫ:
5. Тирамису - 320 руб
6. Чизкейк - 280 руб

НАПИТКИ:
7. Кофе - 150 руб
8. Сок - 120 руб
```

---

## ➡️ Следующий модуль

После завершения переходите к [Module 3: ООП - Часть 1](../module-03-oop-part1/README.md)
